[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "simple-eda-swechchha"
version = "0.1.0"
description = "Tiny EDA helper"
license = { file = "LICENSE" }
dependencies = ["pandas>=1.5"]
